/* src/lib/constants.ts */

// Pulled directly from the Nudgine LLC independent contractor agreement,
// so the app stays in sync if the rate or cap ever changes.
export const HOURLY_RATE_USD = 7;
export const WEEKLY_HOUR_CAP = 40;
export const INVOICE_CYCLE_DAYS = 14;
export const WEEK_STARTS_ON = 1; // Monday